package learningjava;

public class SingleDimentionArray {

	public static void main(String[] args) {

		int[] mySIntArray = new int[10];
		int[] myarray = { 10, 30, 56, 0, 6, 0, 0, 2, 5, 5, 6 };
		System.out.println(myarray.length);

		String[] myStringArray = new String[10];

		System.out.println(myStringArray);

		int array[] = { 5, 2, 8, 7, 1, };
		
		
		
		int arr[] = {5, 2, 8, 7, 1};
		System.out.print("Original array\n");
		for(int i = 0; i<arr.length ; i++)
			System.out.print(arr[i]+"");
		
		
		int temp = 0;
		for(int i = 0; i<arr.length ; i++) {
			for(int j = i+1 ; j < arr.length; j++)
				if(arr[j] < arr[i]) {
					temp = arr[i];
					arr[i] = arr[j];
					arr[j] = temp;
					
				}
		
		}
		System.out.print("\nSorted array\n");
		for(int i = 0; i<arr.length ; i++)
			System.out.print(arr[i]+"");
		
//		--------------------------
		
		System.out.print("Original array\n");
		for(int i = 0; i<arr.length ; i++)
			System.out.print(arr[i]+"");
		
		
		int temp1 = 0;
		for(int i = 0; i<arr.length ; i++) {
			for(int j = i+1 ; j < arr.length; j++)
				if(arr[j] > arr[i]) {
					temp1 = arr[i];
					arr[i] = arr[j];
					arr[j] = temp1;
					
				}
		
		}
		System.out.print("\nSorted array\n");
		for(int i = 0; i<arr.length ; i++)
			System.out.print(arr[i]+"");
		
		
		
		
		
	      int arr[] = { 5, 2, 8, 7, 1 };
	        
	        // Printing the original array
	        System.out.print("Original array: ");
	        for (int i = 0; i < arr.length; i++) {
	            System.out.print(arr[i] + " ");
	        }
	        System.out.println();

	        // Initializing min and max with the first element of the array
	        int min = arr[0];
	        int max = arr[0];
	        
	        // Iterating through the array to find the min and max values
	        for (int i = 1; i < arr.length; i++) {
	            if (arr[i] < min) {
	                min = arr[i];
	            }
	            if (arr[i] > max) {
	                max = arr[i];
	            }
	        }
	        
	        // Printing the min and max values
	        System.out.println("Minimum value: " + min);
	        System.out.println("Maximum value: " + max);
	    }
	}
		
		
		}
	
	
	}


	
			
		
	
	

